'use strict';

const
	{ test, describe, expect } = require( '@jest/globals' ),
	{ generateAjaxTranslationFiles } = require( './ajaxTranslationFilesGenerator.js' );

test( 'ajaxTranslationFilesGenerator generates files', () => {
	expect( true ).toBe( true );
} );
